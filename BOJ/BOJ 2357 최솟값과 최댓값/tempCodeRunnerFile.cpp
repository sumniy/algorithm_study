+ end) / 2, left, right).first,
    //                        FindMinAndMax(tree, node * 2 + 1, (start + end) / 2 + 1, end, left, right).first);
    // tree[node].second = max(FindMinAndMax(tree, node * 2, start, (start + end) / 2, left, right).second,
    //                         FindMinAndMax(tree, node * 2 + 1, (start + end) / 2 + 1, end, left, right).second);